import Foundation
import UniviewNativeCore

/// UniviewAppKit — the AppKit host that mounts a Uniview shadow tree into a
/// native `NSView` hierarchy. Mounting, the component registry, native
/// component factories, and event dispatch land in subsequent tasks.
public enum UniviewAppKit {
    public static let version = "0.0.1"
}
